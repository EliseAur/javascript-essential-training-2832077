/**
 * Note: This file is intentionally empty.
 * You can use it to test your skills at traversing the DOM using JavaScript.
 */

 document.querySelectorAll("main div").forEach(item => item.style.borderWidth = "1em"); 

 document.querySelectorAll("main div h2").forEach(item => item.style.color = "red");
 
 document.querySelectorAll("main div").forEach(item => item.style.borderStyle = "solid");

 document.querySelectorAll("main div").forEach(item => item.style.borderWidth = "0.2em");

 document.querySelectorAll("button").forEach(item => item.style.backgroundColor = "pink");


 document.querySelectorAll("#pack01 form button").forEach(item => item.style.backgroundColor = "green");

 document.querySelectorAll("#pack01 li:nth-child(5) button ").forEach(item => item.style.backgroundColor = "purple");